# Akkous Interface Audit — 2026-05-15 (RÉSOLU)

## Résumé
- Fichiers analysés : 10 (index.html, recipe.html, 404.html, offline.html, style.css, main.js, core.js, ui.js, home.js, recipe.js, sw.js, manifest.webmanifest, recipes.json)
- Bugs trouvés : 13 (🔴 2 Critique / 🟡 4 Moyen / 🟢 7 Mineur)
- Bugs résolus : **13/13** ✅
- État global : ✅ **Tous les bugs corrigés**

## Bugs par sévérité

### 🔴 Critique (bloquant) — Résolus

| # | Fichier | Ligne | Description | Correctif |
|---|---------|-------|-------------|-----------|
| 1 | `style.css` | 2248 & 2374 | **Duplicate `@media print` blocks** — Deux blocs `@media print` coexistent (lignes 2248-2257 et 2374-2440). Le second (plus complet) surcharge partiellement le premier. Les deux utilisent `!important` sur des propriétés `body` contradictoires. Le second utilise des sélecteurs qui ne correspondent à aucune classe réelle. | Premier bloc supprimé (2248-2257). Second bloc fusionné en un seul à la fin de `style.css`. Sélecteurs invalides remplacés par les classes DOM réelles (`.newsletter` → `.newsletter-container`, `.hero-section` → `.recipe-hero`, etc.). |
| 2 | `style.css` | 2470 | **Variable `--primary-soft` non définie** — `.recipe-season { background: var(--primary-soft); }` utilise une variable CSS qui n'est déclarée nulle part. | `--primary-soft: rgba(var(--primary-rgb), 0.15)` défini dans `:root` (ligne 132). S'adapte automatiquement aux deux thèmes. |

### 🟡 Moyen (gênant) — Résolus

| # | Fichier | Ligne | Description | Correctif |
|---|---------|-------|-------------|-----------|
| 3 | `style.css` | 2477-2481 | **Dark mode sur Section 14 utilise `@media (prefers-color-scheme)` au lieu de `[data-theme="dark"]`** — Incohérence avec le toggle manuel. | Blocs `[data-theme="dark"]` ajoutés en parallèle des deux blocs `@media (prefers-color-scheme: dark)` (skeleton ligne 1517, Section 14 ligne 2472). |
| 4 | `404.html` | 53 | **404.html ne charge pas `core.js` / `ui.js`** — Charge uniquement `main.js` (74 KB fallback). | Remplacé `main.js` par `core.js` + `ui.js` + inline init (~20 KB total). `boot()` non appelé → pas de fetch inutile de `recipes.json`. |
| 5 | `404.html` | 53 | **404.html pas de `<noscript>` fallback** | `<noscript>` bloqué ajouté dans `<head>` avec classes `.no-js`/`.js-only`. Div de fallback ajouté entre `<main>` et `<footer>` avec message et lien homepage. |
| 6 | `home.js` | 292 | **Avatar `img.png` peut causer 404** — `siteRootRelativePrefix()` fragile avec URLs `/recipes/`. | Chemin changé en absolu : `"/img.png"` au lieu de `A.siteRootRelativePrefix() + "img.png"`. |

### 🟢 Mineur (cosmétique) — Résolus

| # | Fichier | Ligne | Description | Correctif |
|---|---------|-------|-------------|-----------|
| 7 | `index.html` | 296 | **Texte français dans le composant newsletter** — "Des recettes testées, zéro spam. Désinscription en un clic." | Remplacé par : "Tested recipes, zero spam. Unsubscribe with one click." |
| 8 | `style.css` | 2376-2381 | **Sélecteurs @media print invalides** — Classes inexistantes dans le DOM. | Remplacés par les classes réelles du DOM. |
| 9 | `offline.html` | 31-32 | **Pas de dark mode** — Couleurs hardcodées. | Bloc `@media (prefers-color-scheme: dark)` ajouté avec fond `#1a1a1a`, texte `#e8e8e8`, fond carte `#2a2a2a`. |
| 10 | `manifest.webmanifest` | 25-31 | **Un seul icône SVG dans le manifeste PWA** | Entrées supplémentaires ajoutées pour `192x192` et `512x512` (référençant le SVG existant). |
| 11 | `style.css` | 2249 | **Sélecteur `.back-to-top` mort dans `@media print`** | Supprimé lors de la fusion du bloc @media print. |
| 12 | `style.css` | 711 | **Décalage temporel transition `.recipe-card` vs image** — 0.25s vs 0.3s. | `transition` de `.recipe-card__img img` alignée sur `var(--transition)` (0.25s) pour correspondre à la carte. |
| 13 | `recipe.html` | 148 | **Hero image `fetchpriority="high"` mais `decoding="async"`** — Signaux contradictoires. | `decoding="async"` changé en `decoding="sync"` pour prioriser le décodage LCP. |

## Correctifs appliqués

### style.css
- `--primary-soft: rgba(var(--primary-rgb), 0.15)` ajouté dans `:root` (ligne 132)
- Premier `@media print` supprimé (anciennes lignes 2248-2257)
- Second `@media print` fusionné, sélecteurs corrigés (17 supprimés, 10 corrigés)
- `[data-theme="dark"]` ajouté pour skeleton (ligne 1517) et Section 14 (ligne 2472)
- Transition image carte alignée sur `var(--transition)` (0.25s)

### 404.html
- `main.js` remplacé par `core.js` + `ui.js` + inline init
- Fallback `<noscript>` ajouté

### index.html
- Texte newsletter corrigé (français → anglais)

### offline.html
- Dark mode `@media (prefers-color-scheme: dark)` ajouté

### manifest.webmanifest
- Entrées icônes 192x192 et 512x512 ajoutées

### js/home.js
- Chemin avatar changé en absolu `/img.png`

### recipe.html
- `decoding="sync"` pour l'image héro LCP

## Fichiers sains (aucun bug détecté)
- `js/core.js` — Séparation responsabilités OK, gestion erreur try/catch OK, pas de rendering direct
- `js/ui.js` — Aria-expanded mis à jour, dark mode toggle OK, pas de dépendances home/recipe
- `js/recipe.js` — Nouveaux champs conditionnels OK, gestion erreur recipe not found OK
- `sw.js` — Cache-first assets, stale-while-revalidate recipes.json, network-first HTML, offline fallback OK
- `main.js` — Orchestrateur 783 B, détection page, fallback main.js.backup OK
- `recipes.json` (sample) — Structure valide, champs optionnels présents/absents OK
